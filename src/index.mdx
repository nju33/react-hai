---
route: /
name: Hai
---

import {Playground} from 'docz';
import styled from 'styled-components';
import {HaiProvider, withHai, Hai, HaiStepType, List as HaiList, createHai} from '.';

# Usage

<Playground>
	{() => {
		const NestOfButton = withHai(props => {
			return (
				<div style={{width: '100%', height: '40vh', position: 'relative'}}>
					<button onClick={props.hai.open('hai1')} style={{margin: '2em'}}>button1</button>
					<button onClick={props.hai.open('hai2')} style={{margin: '1em 8em'}}>button2</button>
					<button onClick={props.hai.open('hai3')} style={{position: 'absolute', right: 0, top: 10}}>button3</button>
					<button onClick={props.hai.open('hai4')} style={{position: 'absolute', right: 30, bottom: 10}}>button4</button>
					<button onClick={props.hai.open('hai5')} style={{position: 'fixed', right: '0', top: '30'}}>button5</button>
					<button onClick={props.hai.open('hai6')} style={{position: 'fixed', right: 100, bottom: '0'}}>button6</button>
					<button onClick={props.hai.open('hai7')} style={{position: 'fixed', left: 300, top: 100}}>button7</button>
				</div>
			)
		});

		const MyHai = createHai({
			List: styled(HaiList)`
				background: #333;
				color: #fff;
			`,
		});

		return (
			<HaiProvider>
				<>
					<NestOfButton />
				</>
				<Hai
					id="hai1"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fuga',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
				<Hai
					id="hai3"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fuga',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
				<Hai
					id="hai2"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fuge',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
				<Hai
					id="hai4"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fuga',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
				<Hai
					id="hai5"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fugaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
				<Hai
					id="hai6"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fugaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
				<MyHai
					id="hai7"
					steps={[
						{
							type: HaiStepType.List,
							items: [
								'foo',
								'bar',
								'baz'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return 1;
							}
						},
						{
							type: HaiStepType.List,
							items: [
								'hoge',
								'fugaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa',
								'bazu'
							],
							onClick: itemIndex => {
								{/* console.log(itemIndex); */}
								return;
							}
						}
					]}
				/>
			</HaiProvider>
		)
	}}
</Playground>
